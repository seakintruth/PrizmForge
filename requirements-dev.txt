# PrizmForge development / test dependencies
# Assumes Advana Nexus availability for popular DS/DE packages.

-r requirements.txt

# Testing (required for full suite)
pytest>=7.4.0
pytest-mock>=3.12.0

# Optional: parallel / timeout plugins (not required)
# pytest-timeout>=2.2.0
# pytest-xdist>=3.5.0

# Optional: code quality (recommended on workstations; not required to run tests)
black>=23.0.0
flake8>=6.1.0

# Optional: only if present on your mirror
# isort>=5.13.0
# pylint>=3.0.0
# coverage>=7.0.0
