# PrizmForge development / test dependencies
# Assumes Advana Nexus availability for popular DS/DE packages.

-r requirements.txt

# Testing (required for full suite)
pytest>=9.1.1
pytest-mock>=3.12.0

# Optional: parallel / timeout plugins (not required)
pytest-timeout>=2.2.0
pytest-xdist>=3.5.0

# Code quality (recommended)
black>=26.5.1
flake8>=7.3.0
isort>=8.0.1

# Lint / fast fixer
ruff>=0.16.0

# Type checking
mypy>=1.5.0

# Optional
pylint>=3.0.0
coverage>=7.0.0

# for Utils/run_codemodes.py
libcst>=1.9.0