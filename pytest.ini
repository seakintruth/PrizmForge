[pytest]
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
testpaths = tests

# Prefer utils/run_fast_tests.sh for the curated fast gate.
# marker reserved for optional future tagging
# markers already include slow
