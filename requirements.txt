# PrizmForge runtime dependencies
# Install from Advana Nexus / SageMaker-compatible index when applicable.
#
# Runtime surface is intentionally minimal for hardened environments.

requests>=2.34.2

# gitignore-aware file filtering (core/gitignore.py)
pathspec>=0.12.1
